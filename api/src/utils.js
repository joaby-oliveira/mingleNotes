const utils = {
    isEmpty: (str)=>{
        if(str != '') {
            return false;
        }else {
            return true;
        }
    }

    // IsEmpty
    // IsOnlyString
    // IsOnlyNumber
    // IsValidCpf
    // IsOnceSpace
    // IsValidEmail
    // IsValidPassword
    // isValidLength
    // isOnlyLetter

}

module.exports = utils;